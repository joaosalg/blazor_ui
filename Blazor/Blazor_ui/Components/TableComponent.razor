@typeparam T;

<h3></h3>

<table class="table table-striped">
	<thead>
		<tr>
			<th>#</th>
			<th>Nome</th>
			<th>Total</th>
		</tr>
	</thead>
	<tbody>
		@if(listT != null) 
		{
		    @foreach (var listT in listT)
		    {
			    <tr>
				    <th></th>
				    <th></th>
				    <th></th>
			    </tr>
		    }
		}
	</tbody>
</table>

@code {

	[Parameter]
	public IEnumerable<ProdutoDTO> produtoDTO { get; set; } = new List<ProdutoDTO>();

	[Parameter]
	public IEnumerable<PedidoDTO> pedidoDTO { get; set; } = new List<PedidoDTO>();

	[Parameter]
	public IEnumerable<T> listT { get; set; } = new List<T>();			
}
